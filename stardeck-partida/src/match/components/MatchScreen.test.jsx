import { expect } from "vitest";
import { render, screen } from "@testing-library/react";


describe("MatchScreen", () => {
  test("should show the matchScreen", () => {
    expect(true).toBe(true);
  });
});
