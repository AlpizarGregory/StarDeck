import { expect } from "vitest";
import { render, screen } from "@testing-library/react";


describe("MatchCountdown", () => {
  test("should show the matchCountdown", () => {
    expect(true).toBe(true);
  });
});
